var a = 3;
var b = 5;

//Toda vez que A for igual a A, ela ficará igual 5.
if (a === a) {
    a = 5;
}

//Toda vez que B for igual a B, ela ficará igual a 3.
if (b === b) {
    b = 3;
}

console.log ("Variável a alterada: " + a);
console.log ("Variável b alterada: " + b);